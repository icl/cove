<%= form_for(@videocode) do |f| %>
  <% if @videocode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@videocode.errors.count, "error") %> prohibited this videocode from being saved:</h2>

      <ul>
      <% @videocode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :video_id %><br />
    <%= f.text_field :video_id %>
  </div>
  <div class="field">
    <%= f.label :code_id %><br />
    <%= f.text_field :code_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
