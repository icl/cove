<%= form_for(@jobvideo) do |f| %>
  <% if @jobvideo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jobvideo.errors.count, "error") %> prohibited this jobvideo from being saved:</h2>

      <ul>
      <% @jobvideo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_id %><br />
    <%= f.text_field :job_id %>
  </div>
  <div class="field">
    <%= f.label :video_id %><br />
    <%= f.text_field :video_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
